// Opens the backend and the frontend as one workspace. The frontend carries its own
// VS Code settings, so it has to be a separate folder entry rather than a subfolder.
{
  "folders": [
    { "path": ".." },
    { "path": "../frontend" }
  ],
  "settings": {}
}
